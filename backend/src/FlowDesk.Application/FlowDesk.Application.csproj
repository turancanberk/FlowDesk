<Project Sdk="Microsoft.NET.Sdk">

  <!--
    Application yalnızca Domain'e bağımlıdır. Altyapı ihtiyaçları burada
    tanımlanan arayüzlerle ifade edilir; uygulamaları Infrastructure sağlar.

    EF Core buradaki tek istisnadır ve bilinçlidir (ADR-0021): IFlowDeskDbContext
    sözleşmesi DbSet<T> döndürür, böylece projeksiyon ve sorgu bileşimi
    kaybolmaz. Sağlayıcıya (Npgsql) bağımlılık burada YOKTUR; bu sınır mimari
    testlerle korunur.
  -->
  <ItemGroup>
    <ProjectReference Include="..\FlowDesk.Domain\FlowDesk.Domain.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.EntityFrameworkCore" />
    <PackageReference Include="FluentValidation" />
  </ItemGroup>

</Project>
